name: build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ---------------------------------------------------------------- tests
  selftest:
    name: content self-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install Tk (needed for the UI smoke test)
        run: sudo apt-get update && sudo apt-get install -y tk xvfb

      - name: Every reference solution must pass its own tests (English)
        run: python selftest.py 2 en

      - name: ... and German, plus translation coverage
        run: python selftest.py 2 de

      - name: UI smoke test
        run: xvfb-run -a python tools/gui_smoke.py

  # --------------------------------------------------------------- builds
  build:
    name: ${{ matrix.label }}
    needs: selftest
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: windows-x64
            archive: CodeForge-windows-x64.zip
          - os: ubuntu-latest
            label: linux-x64
            archive: CodeForge-linux-x64.tar.gz
          - os: macos-latest
            label: macos-arm64
            archive: CodeForge-macos-arm64.zip
          # No macos-13 (Intel) job: those runners are being retired and queue
          # indefinitely, which would block every release. Intel Mac users can
          # build locally with `pyinstaller CodeForge.spec` or run from source.

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install Tk (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y tk

      - name: tkinter must be importable
        run: python -c "import tkinter; print('Tk', tkinter.TkVersion)"

      - name: Install PyInstaller
        run: python -m pip install --upgrade pip pyinstaller

      - name: Build
        run: pyinstaller --noconfirm CodeForge.spec

      - name: Verify the frozen build can actually run user code
        shell: bash
        run: python tools/verify_frozen.py

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path dist/CodeForge/* -DestinationPath ${{ matrix.archive }}

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: tar -czf ${{ matrix.archive }} -C dist CodeForge

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: ditto -c -k --keepParent dist/CodeForge.app ${{ matrix.archive }}

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.label }}
          path: ${{ matrix.archive }}
          if-no-files-found: error

  # -------------------------------------------------------------- release
  release:
    name: publish release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: List what we are about to publish
        run: ls -lh artifacts

      - name: Create the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --title "CodeForge ${GITHUB_REF_NAME}" \
            --generate-notes \
            artifacts/*
